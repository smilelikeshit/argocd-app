apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fruits-ingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /orange
            backend:
              serviceName: orange-service.orange.cluster.svc.local
              servicePort: 5678
