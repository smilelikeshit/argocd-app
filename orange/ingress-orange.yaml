apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fruitsingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /orange
            backend:
              serviceName: orangeservice.orange.cluster.svc.local
              servicePort: 5678
